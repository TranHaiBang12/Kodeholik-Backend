spring:
  datasource:
    url: jdbc:h2://mem:db;DB_COLSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    ddl-auto: create-drop  
    show-sql: false  
  sql:
    init:
      mode: always
